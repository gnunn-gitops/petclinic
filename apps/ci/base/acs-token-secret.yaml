apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: acs-api-token
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: petclinic
  data:
  - secretKey: token
    remoteRef:
      key: ACS_TOKEN
